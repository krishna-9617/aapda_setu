node_modules/
dist/
__pycache__/
*.pyc
.pytest_cache/
.venv/
venv/

# Derived artifacts and caches are regenerated by the geo.* scripts.
# Committed copies go stale silently, which is worse than a missing file.
backend/data/cache/

# Local AI-agent tooling (skills, caches) - not part of the project.
.agents/
